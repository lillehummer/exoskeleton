var HomeSlider = (function () {
    var s;

    return {
        settings: {
        },

        init: function() {
            s = this.settings;
            this.runSlider();
        },

        runSlider: function() {
        }

    };
})();

jQuery(document).ready(function($) {

});
